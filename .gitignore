# WoW writes these next to the addon; they are not source.
*.bak
*.orig
.DS_Store
Thumbs.db

# Rendered artwork is committed; the source script lives in the repo history.

# Working notes and the local release helper: mine, not the addon's.
PUBLISHING.md
release.ps1
